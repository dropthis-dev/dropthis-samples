# Shadow Dodger

Meme-based arcade game: you are the small shadow under the bridge, dodging
lane traffic. Generated 8-bit chiptune via Web Audio, touch + keyboard
controls, single HTML file. Open `index.html` in a browser.

`checks.test.mjs` holds static content checks: `node checks.test.mjs`.
