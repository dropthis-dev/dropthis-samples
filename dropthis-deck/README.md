# dropthis in six slides

The pitch deck for dropthis itself — animated, keyboard- and swipe-driven,
a self-contained HTML file plus three showcase screenshots. Hook → problem →
the command → live showcase (report, game, photo wall) → the surfaces →
"point your agent here".

Open `index.html` in a browser. Navigate with arrow keys / space (desktop)
or swipe (mobile). Slides deep-link via `#1`–`#6`.

This is the suggested **first drop** in [`AGENTS.md`](../AGENTS.md): the
deck that explains dropthis, published by dropthis.
