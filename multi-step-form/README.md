# Get Early Access — multi-step form

Multi-step signup form demo in one HTML file — progress bar, validation,
client-side state. Originally shipped as the `dropthis` CLI example.
Open `index.html` in a browser.
