# Local Asteroids

Canvas Asteroids clone with a persistent local leaderboard (`localStorage`),
all in one HTML file. Open `index.html` in a browser; arrow keys + space.

`checks.test.mjs` holds static content checks: `node checks.test.mjs`.
